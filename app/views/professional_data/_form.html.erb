<%= form_with(model: professional_datum, local: true, url: employee_professional_data_path(@employee)) do |form| %>
  <% if professional_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professional_datum.errors.count, "error") %> prohibited this professional_datum from being saved:</h2>

      <ul>
        <% professional_datum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.fields_for :title, OpenStruct.new(@professional_datum.title) do |title_fields| %>
      <%= title_fields.label :title_ar %>
      <%= title_fields.text_field :ar %>
      <%= title_fields.label :title_en %>
      <%= title_fields.text_field :en %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :started_at %>
    <%= form.datetime_select :started_at %>
  </div>

  <div class="field">
    <%= form.label :ended_at %>
    <%= form.datetime_select :ended_at %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id, value: @employee.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
